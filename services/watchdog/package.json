{
  "name": "adesa-watchdog",
  "version": "1.0.0",
  "description": "Watchdog for cars in Adesa.eu",
  "main": "index.js",
  "private": false,
  "author": "Juraj Zec (https://github.com/xzec)",
  "license": "MIT",
  "scripts": {
    "invoke:local:dev": "sls invoke local -f adesaWatchdog -s dev",
    "invoke:dev": "sls invoke -f adesaWatchdog -s dev",
    "deploy:code:dev": "sls deploy function -f adesaWatchdog -s dev",
    "deploy:dev": "sls deploy -s dev"
  },
  "devDependencies": {
    "@types/node": "^20.2.5",
    "eslint-config-adesa": "workspace:*",
    "tsconfig": "workspace:*",
    "typescript": "^5.0.4"
  },
  "dependencies": {
    "node-fetch": "^3.3.1",
    "zod": "^3.21.4"
  }
}
